<h1>Emprego Já</h1>
<section id="jobs">
<% @jobs.each do |job| %>
  <div>
    <h1><%= job.title %></h1>
    <p>
      <%= job.location %><br>
      <%= job.company.name %><br>
      <%= link_to 'Ver mais', job %>
    </p>
  </div>
<% end %>
</section>
<aside>
  <% @companies.each do |company| %>
    <%= link_to company.name, company %>
  <% end %>
</aside>
